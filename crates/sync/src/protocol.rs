//! Synchronization protocol

use ulw_core::Result;

pub struct SyncProtocol {
    // Placeholder
}

impl SyncProtocol {
    pub fn new() -> Result<Self> {
        Ok(Self {})
    }

    pub async fn sync(&self) -> Result<()> {
        Ok(())
    }
}
